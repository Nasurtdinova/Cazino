<Page x:Class="PLay.RegistrationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PLay"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RegistrationPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Name"></TextBlock>
        <TextBlock Text="Login" Grid.Row="1"></TextBlock>
        <TextBlock Text="Password" Grid.Row="2"></TextBlock>
        <TextBox Grid.Column="1" Name="tbName" Text="{Binding Name}"></TextBox>
        <TextBox Grid.Column="1" Grid.Row="1" Name="tbLogin" Text="{Binding Login}"></TextBox>
        <PasswordBox Grid.Column="1" Grid.Row="2" Name="tbPassword"></PasswordBox>
        <Button Grid.Row="3" Grid.ColumnSpan="2" Content="Зарегистрироваться" Name="btnREgistr" Click="btnREgistr_Click"></Button>
    </Grid>
</Page>
